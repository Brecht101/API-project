version: "3.9"
services:
 randomizer-service:
  image: brecht404/python-api
  ports:
    - "8000:8000"
 database:
   image: mysql
   environment:
     MYSQL_ROOT_PASSWORD: passW1$!
     MYSQL_DATABASE: users
